class Journey{
    constructor (start, end, mode, distance) {
       this.start = start;
       this.end = end;
       this.mode = mode;
       this.distance = distance;
    };
}

module.exports = Journey;